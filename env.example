# 環境変数テンプレ（このファイル自体はコミット対象）
#
# 使い方:
# 1) このファイルをコピーして `.env.local` を作成
# 2) `SHEET_CSV_URL` に Google Sheets の「CSV公開URL」を設定
# 3) `npm run dev` を再起動（起動中の場合は Ctrl+C → 再実行）
#
# 例:
# https://docs.google.com/spreadsheets/d/e/<PUBLISH_ID>/pub?gid=0&single=true&output=csv
SHEET_CSV_URL=

